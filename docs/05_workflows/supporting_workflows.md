# Supporting Workflows

Date: `2026-04-05`
Repo: `QuantOps_github`
Status: `supporting_workflow_index`

## Purpose

This file routes to workflow-oriented documents that still remain at the root of `docs/`.

They are still useful, but the canonical workflow entrypoint is this folder.

## Root-Level Workflow Docs

- `../development-workflow.md`
- `../correlation-logging-guide.md`

## Neighboring Runtime / Operator Docs

These are adjacent to workflows, but belong primarily to runtime operations rather than workflow canon:

- `../ops-runbook.md`
- `../SprintH_completion_report.md`

## Rule For Use

Use the canonical files in `05_workflows/` first when deciding process flow.

Use the root-level docs above when:

- you need older workflow context
- you need deeper tracing details
- you need runtime/operator investigation detail that the canonical workflow summary does not yet include
