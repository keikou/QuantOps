# Current Tasks

Date: `2026-04-02`
Repo: `QuantOps_github`
Branch: `codex/post-phase7-hardening`
Track: `research_promotion_intelligence_v1`
Status: `active`

## Purpose

This file is the canonical current-task entrypoint for both humans and AI agents.

It answers:

- what is active now
- what is explicitly not active now
- what should happen next if work resumes

## Current State

The current hardening slice is already treated as sufficiently complete.

That means:

- do not restart `Cross-Phase Acceptance`
- do not reopen `Recovery / Replay Confidence`
- do not deepen resume/handover packaging unless a real regression is found

## Active Task

Current top task:

- start `Research / Promotion Intelligence`

Current architect-selected candidate:

- `Research / Promotion Intelligence`
- current packet RPI-06 = `persisted governed state transition`
- current dependency 1 = `Alpha / Strategy Selection Intelligence v1 checkpoint through ASI-05`
- current dependency 2 = `Portfolio Intelligence v1 checkpoint through PI-05`
- current dependency 3 = `Governance -> Runtime Control v1 checkpoint through C6`

## Why This Is The Active Task

Architect now treats `Execution Reality v1` as completed enough for checkpoint/baseline/report.

So the next question is no longer:

- "can we measure execution well enough?"
- "can runtime control resolve conflicts well enough?"

The next question is:

- "what should advance?"
- "what should stay queued?"
- "what should be promoted, demoted, or retired?"
- "how should the effective selection slate become a research/promotion agenda?"
- "how should that agenda be tied to experiment/model lineage for review?"
- "how should lineage-backed candidates be split into explicit review queues?"
- "how should review queues resolve into one board-facing decision slate?"
- "how should board-facing decisions resolve into deterministic applied outcomes?"
- "how should deterministic applied outcomes become persisted governed state transitions?"

## Next Candidate Options

1. `Governance -> Runtime Control`
   - now treated as completed enough for first checkpoint and handoff
2. `Execution Reality`
   - now treated as checkpointed baseline, not active lane
3. `Portfolio Intelligence`
   - now treated as completed enough for first checkpoint and handoff
4. `Alpha / Strategy Selection Intelligence`
   - now treated as completed enough for first checkpoint and handoff
5. `Research / Promotion Intelligence`
   - now the current first choice

## Current Recommendation

Use `Research / Promotion Intelligence` as the current active lane.

## Explicit Non-Tasks

These are not current tasks:

- replaying old hardening packet order
- creating another acceptance-only lane
- re-packaging resume docs again
- reopening `Phase1` to `Phase7` closure work
- deepening `Execution Reality` before alpha selection begins consuming existing downstream evidence
- deepening `Governance -> Runtime Control` before alpha selection consumes it
- deepening `Portfolio Intelligence` before alpha selection begins
- deepening `Alpha / Strategy Selection Intelligence` before research/promotion consumes it

## Inputs To Read Before Acting

1. `../00_index/README.md`
2. `../Architect_alignment_resume_memo_2026-04-02.md`
3. `../Post_Phase7_hardening_architect_report_2026-04-02.md`
4. `../Auto_resume_handover_2026-04-02.md`

## Expected Output Of The Next Task

The current lane should begin with:

- one plan doc
- one verifier script
- one narrow first invariant or selection target

## Single-Block Resume Note

```text
Current task is not another hardening packet, not another Execution Reality packet, not another Governance -> Runtime Control packet, not another Portfolio Intelligence packet, and not another Alpha / Strategy Selection Intelligence packet. Current task is Research / Promotion Intelligence Packet 06. Deterministic applied review outcomes should resolve into persisted governed state transitions.
```
